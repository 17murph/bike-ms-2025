// This component is no longer used, but we'll keep it for reference
// All social media sharing is now handled by the SocialMediaLinks component at the bottom of each page

export function SocialShare({ title, description, compact = false }) {
  return null // Return null to effectively remove this component from all pages
}

